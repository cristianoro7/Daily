<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data class="ThemesBinding">

        <variable
            name="model"
            type="desperado.com.daily.presentation.themes.viewmodel.ThemeViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#f3f3f3">

        <android.support.v7.widget.Toolbar
            android:id="@+id/themes_tb_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/colorPrimary"
            app:navigationIcon="@drawable/menu"
            app:title="@{model.mTitle}" />

        <android.support.v4.widget.SwipeRefreshLayout
            android:id="@+id/themes_srl_refresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <desperado.com.daily.presentation.ui.CustomRecyclerView
                android:id="@+id/themes_rv_recycler_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:adapter="@{model.getThemeAdapter()}"
                app:layout_manager="@{model.getLayoutManager()}" />

        </android.support.v4.widget.SwipeRefreshLayout>

    </LinearLayout>
</layout>